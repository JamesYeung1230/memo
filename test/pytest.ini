[pytest]
testpaths = tests
addopts = -p no:junitxml
markers =
    smoke: Smoke test for critical paths
    auth: Auth service API tests
    wechat: WeChat mini-program related tests
    admin: Admin-related tests
    knowledge: Knowledge service CRUD tests

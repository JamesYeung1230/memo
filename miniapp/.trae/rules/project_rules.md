# 码上启航 微信小程序 — Part B 扩展：AI 编程行为规范

> 本文件是对根规则 [Part B — AI 编程行为规范](root-rules.md#part-b--ai-编程行为规范) 的子项目扩展。
> 根规则 Part B 的通用条款（B.1 上下文感知 ~ B.8 任务追踪）同样适用，不重复列出。
> 根规则 Part A（Git 工作流）、Part C（Skill 管理）、Part D（规则维护指南）、Part E（MCP 规范）、Part F（Agent 规范）、Part G（Token 开销优化规范）全局适用，本文件不涉及。

---

## 规则定位

| 维度 | 说明 |
|------|------|
| 所属项目 | miniapp |
| 对应根规则 | [Part B — AI 编程行为规范](../../.trae/rules/project_rules.md#part-b--ai-编程行为规范) |
| 基础条款 | B.1 上下文感知 ~ B.8 任务追踪（通用，本文件不重复） |
| 扩展条款 | B.9 ~ B.14（本文件定义的小程序特定条款） |

---

## B.9 上下文感知（小程序补充）

- 了解项目中已有的组件、工具函数、页面结构后再进行编码，避免重复造轮子
- 每次任务开始前必须加载以下锚点文档：
  1. [project.md](../../project.md) — 项目背景、当前阶段、技术栈
  2. [本文件](project_rules.md) — 小程序 Part B 扩展

## B.10 修改验证（小程序补充）

- 每次修改完成后，必须运行对应的 lint 命令进行验证（如 `eslint` 等）
- 修改涉及的功能应在微信开发者工具中验证 UI 还原度和交互正确性
- WXML/WXSS 修改后应确保页面布局在不同屏幕尺寸下正常显示

## B.11 依赖管理（小程序补充）

- 使用新依赖前，先检查 `package.json`、`project.config.json` 等文件中是否已有替代方案
- 微信小程序插件需确认与目标基础库版本的兼容性

## B.12 安全红线（小程序补充）

- 禁止在小程序端存储用户密码等敏感信息
- 涉及用户数据操作时，必须遵循最小数据原则，仅请求必要权限
- 网络请求必须使用 HTTPS，禁止明文 HTTP 请求
- 用户输入必须做安全过滤，防止 XSS 攻击

## B.13 测试覆盖（小程序补充）

- 每个功能完成后，必须编写对应的测试用例或测试指引
- 交互行为测试应涵盖不同机型屏幕适配

## B.14 UI 还原与交互

- 所有 UI 实现严格参照交互设计文档和 PRD 中的设计规范
- 主题色、圆角、字体等设计 Token 以设计规范定义值为准，禁止硬编码色值
- 页面交互状态（加载中、空数据、错误、正常）必须完整覆盖
- 微信小程序原生 API 调用需做好兼容处理（基础库版本判断）

# 项目总览

## 项目信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 卡片学记（知识卡片学习小程序） |
| 项目类型 | 微信小程序（纯前端） |
| 技术栈 | 微信原生框架 + Skyline 渲染引擎 + glass-easel 组件框架 |
| 设计规范 | 基于 Pixso 设计稿，深紫色 (#160C57) 品牌色，30px 大圆角，8px 描边 |
| 当前阶段 | **项目初始化（未开发）** |
| 代码仓库 | [https://github.com/JamesYeung1230/memo.git](https://github.com/JamesYeung1230/memo.git) |
| 相关文档 | [PRD.md](docs/PRD.md)、[交互设计.md](docs/交互设计.md) |

## 项目结构

```
miniapp/
├── app.js                     # 小程序入口
├── app.json                   # 全局配置（页面路由、窗口样式、渲染引擎）
├── app.wxss                   # 全局样式
├── project.config.json        # 微信开发者工具配置
├── project.private.config.json
├── sitemap.json               # 站点地图（SEO 配置）
├── .eslintrc.js               # ESLint 配置
├── components/
│   └── navigation-bar/        # 自定义导航栏组件
│       ├── navigation-bar.js
│       ├── navigation-bar.json
│       ├── navigation-bar.wxml
│       └── navigation-bar.wxss
├── pages/
│   └── index/                 # 首页（当前仅占位）
│       ├── index.js
│       ├── index.json
│       ├── index.wxml
│       └── index.wxss
├── docs/
│   ├── PRD.md                 # 产品需求文档
│   └── 交互设计.md             # 交互设计文档
├── project.md                 # 本文件
└── README.md                  # 项目说明
```

## 开发里程碑

| 阶段 | 里程碑 | 状态 | 开始日期 | 完成日期 | 说明 |
|------|--------|------|----------|----------|------|
| **M0** | 项目初始化 | 🔴 进行中 | 2026-05-04 | - | 项目配置、文档、设计稿 |
| **M1** | 学习首页 MVP | ⚪ 待开始 | - | - | 首页 Tab + 今日待复习列表 + 统计卡片 |
| **M2** | 卡片学习模式 | ⚪ 待开始 | - | - | 卡片浏览 + 3D 翻转动画 + 掌握度标记 |
| **M3** | 测验模式 | ⚪ 待开始 | - | - | 选择题作答 + 对错反馈 + 解析面板 |
| **M4** | 测验结果页 | ⚪ 待开始 | - | - | 分数圆环 + 统计卡片 + 错题回顾 |
| **M5** | 知识库管理 | ⚪ 待开始 | - | - | 分类浏览 + 卡片创建 + 搜索 |
| **M6** | 个人中心 | ⚪ 待开始 | - | - | 学习统计 + 打卡日历 + 功能菜单 |
| **M7** | 数据持久化 | ⚪ 待开始 | - | - | 本地存储 + 学习记录 |
| **M8** | V1.0 提测 | ⚪ 待开始 | - | - | 完整测试 + 修复 |

📌 状态说明：🔴 进行中 / 🟡 阻塞 / 🟢 已完成 / ⚪ 待开始

---

## 强制要求

### 1. 上下文加载
每次执行任务前，**必须先加载 [project.md](project.md)** 到上下文中，了解项目背景、当前阶段和技术栈。

### 2. 意图确认
收到任务后，**仔细分析用户意图**。如果存在以下情况，必须**反问用户确认**后再执行：
- 需求描述模糊，存在多种理解方式
- 与现有产品逻辑可能存在冲突
- 技术方案存在多种可选路径
- 任务的边界范围不清晰

### 3. Agent 协同
执行任务时，在可用的 Agent 配置中选取合适的 Agent。当任务可以拆分为多个独立子任务时，**协同调用多个 Agent 并行工作**，提升任务效率。

可用 Agent：
- `frontend-architect` — 前端开发、UI 实现
- `ui-designer` — 交互设计、视觉还原
- `backend-architect` — 后端 API 设计
- `search` — 代码搜索与调研

### 4. Git 本地提交
**每次修改完成后必须维护 git 仓库**，将代码修改记录提交到本地仓库。提交信息需清晰描述本次变更内容。

### 5. 禁止擅自推送远程
**用户未主动要求，不得擅自提交到远程仓库。** 所有 git 操作仅限于本地仓库。

### 6. README 维护
在工作目录维护 [README.md](README.md)，说明该目录下项目内容的情况。项目结构或功能有变更时同步更新。

### 7. 文件修改边界
**所有 Agent 以及所有修改操作，只能修改当前项目文件夹 `d:\workspace\memo\miniapp\` 下的文件，不能修改本项目文件夹外的其他文件。**

---

## 核心原则：纯前端项目

这是一个**纯粹的前端项目**（微信小程序），不包含后端服务。

### 后端调用规范

在遇到需要调用后端接口的场景时：
1. **严格依照后端 SDK 文档**进行调用
2. 如果发现后端 SDK 文档与后端实际提供的 SDK **不一致**：
   - **坚持以文档为准**
   - **不得**直接使用后端 SDK 中提供的不符合规范的 SDK
   - 将问题原因报告为**后端问题**，由后端团队修正
3. **不要**强行改变前端业务逻辑来适配后端的错误实现

### 设计还原规范

1. 所有 UI 实现严格参照 [交互设计.md](docs/交互设计.md) 中的设计规范
2. 设计 Token（颜色、圆角、阴影、字体）统一使用第 6 章定义的值
3. Pixso 设计稿为视觉还原的最高参照标准

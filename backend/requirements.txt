# ============================================================
# 码上启航后端 — 全服务开发/测试依赖（单机开发用）
# 各服务独立部署时请使用 services/*/requirements.txt
# ============================================================

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- 数据库 ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# --- 数据校验 ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- 缓存 ---
redis[hiredis]>=5.0.0

# --- HTTP 客户端 ---
httpx>=0.27.0

# --- 认证 ---
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# --- 环境配置 ---
python-dotenv>=1.0.0

# --- 日志 ---
loguru>=0.7.0

# --- AI（Knowledge 服务）---
openai>=1.0.0

# --- 异步任务（Knowledge 服务）---
arq>=0.26.0

# --- 文件上传（Core 服务）---
python-magic>=0.4.27
Pillow>=10.0.0

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
